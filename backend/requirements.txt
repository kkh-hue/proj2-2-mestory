fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
python-dotenv>=1.0
openai>=1.60          # OpenRouter도 이 SDK로 부릅니다(base_url만 교체)
langfuse>=4.0          # 필수 조건 4. 3.x부터 CallbackHandler가 langfuse.callback → langfuse.langchain으로
                        # 옮겨지고 생성자 인자(session_id 등)도 바뀌었음 — 코드는 이 새 API 기준으로 작성돼 있음
mcp>=1.29,<2           # 필수 조건 5 — FastMCP. mcp 2.x부터 FastMCP가 MCPServer로 이름이 바뀌고
                        # mcp.server.fastmcp가 없어져서, mcp_server/server.py(v1 API)가 깨진다
pandas>=2.2            # mcp_server/tools가 씀 — 같은 컨테이너에서 서브프로세스로 띄우므로 여기에도 필요
langchain>=0.3,<1.0        # 1.0부터 create_tool_calling_agent/AgentExecutor가 사라지고
                            # 그 자리를 대체한 create_agent는 내부적으로 LangGraph를 쓴다 —
                            # 이 프로젝트는 LangGraph 금지라 0.3.x(마지막 pre-1.0)에 고정한다.
langchain-openai>=0.2,<1.0  # 위 langchain과 같은 라인으로 고정
langchain-mcp-adapters>=0.1,<0.2  # 0.2 이상은 langchain-core 1.x 전용 messages.content 서브모듈을 요구해서 0.1.x로 낮춤
psycopg[binary]>=3.2    # mcp_server/tools/data_loader.py의 DB 모드(MESTORY_DATA_SOURCE=db)가 씀 —
                        # 같은 컨테이너에서 서브프로세스로 띄우므로 여기에도 필요
# asyncpg>=0.30       # Postgres를 쓴다면
